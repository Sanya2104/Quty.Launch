<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================================ -->
    <!-- РАЗРЕШЕНИЯ                                                    -->
    <!-- ============================================================ -->

    <!-- Для получения информации о телефоне (уровень сигнала, тип сети) -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Для проверки состояния сети -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Для проверки состояния Wi-Fi -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Для доступа в интернет (проверка обновлений, загрузка оболочек) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Для определения местоположения (статус GPS) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- ДЛЯ УСТАНОВКИ APK (Android 8+) -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"
        tools:ignore="RequestInstallPackagesPolicy" />

    <!-- ============================================================ -->
    <!-- QUERIES                                                       -->
    <!-- ============================================================ -->

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <!-- ============================================================ -->
    <!-- ПРИЛОЖЕНИЕ                                                    -->
    <!-- ============================================================ -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QutyLaunch"
        android:requestLegacyExternalStorage="false"
        tools:ignore="UnusedAttribute">

        <!-- ============================================================ -->
        <!-- АКТИВНОСТИ                                                    -->
        <!-- ============================================================ -->

        <!-- Главный экран -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
        </activity>

        <!-- Экран приветствия (онбординг) -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="true"
            android:theme="@style/Theme.QutyLaunch"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

        <!-- Экран настроек -->
        <activity
            android:name=".ParametersActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.AppCompat.Translucent">
        </activity>

        <!-- Логгер (доступен только в DevMode) -->
        <activity
            android:name=".LoggerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.AppCompat.Translucent">
        </activity>

        <!-- Магазин -->
        <activity
            android:name=".StoreActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.AppCompat.Translucent">
        </activity>

        <!-- ============================================================ -->
        <!-- FILE PROVIDER                                                 -->
        <!-- ============================================================ -->

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>