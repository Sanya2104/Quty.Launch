<!-- res/layout/fragment_settings_shell.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background_dark"
    android:padding="16dp"
    tools:ignore="VisualLintBounds">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/settings_shell_title"
        android:textColor="@color/text_secondary"
        android:textSize="16sp"
        android:padding="16dp"
        android:paddingTop="8dp"/>

    <ListView
        android:id="@+id/shells_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/background_card"
        android:divider="@color/divider"
        android:dividerHeight="1dp"
        android:choiceMode="singleChoice"/>

    <!-- Кнопка установки оболочки из файла -->
    <Button
        android:id="@+id/btn_install_shell"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="16dp"
        android:text="@string/install_shell_from_file"
        android:textColor="@color/text_primary"
        android:textSize="16sp"
        android:fontFamily="sans-serif-medium"
        android:backgroundTint="@color/button_primary"
        app:cornerRadius="28dp"
        app:rippleColor="#33FFFFFF" />

</LinearLayout>